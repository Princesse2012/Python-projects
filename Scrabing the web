import urllib.request

fhand = urllib.request.urlopen('http://data.pr4e.org/romeo.txt')
for line in fhand:
    print(line.decode().strip())
    
   ################################
   import urllib.request

fhand = urllib.request.urlopen('https://data.pr4e.org/words.txt')
for line in fhand:
    print(line.decode().strip())
